import React from 'react';
import PropTypes from 'prop-types';

page.propTypes = {
    
};

function page(props) {
    return (
        <div>
            Page is not available
        </div>
    );
}

export default page;